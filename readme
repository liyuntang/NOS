1、NOS是一个分布式对象存储系统，该系统氛围四个组件：
    1）client：由于我们在该系统中设置了数据校验规则所以这里根据校验规则单独开发了客户端程序，该客户端包含以下两个部分：
        A）get：下载object程序
        B)put：上传object程序，该程序支持上传文件及随机上传文件，随机上传的话数据依赖data.file文件
    1）nosServer：提供接口服务，对象响应请求
    2）pdServer：该系统的修复组件，在系统中起到调控的作用
    3）kvServer：提供数据服务，负责真正的数据存储

2、该系统设计原则：
    1）借鉴微服务架构各个组件之间低耦合性，nosServer、pdServer、kvServer三个组件独立运行，互不干扰
    2）使用etcd服务实现高可用、负载均衡，放弃传统的高可用、负载均衡架构
    3）各个组件专注于自己的功能，不越权，比如kvserver只负责对象存储，对象数据的容灾、修复、平衡交给pdServer

3、该系统依赖以下几个服务：
    1）一个存放元数据信息的数据表，mysql、tidb均可，建表语句在support-files/metadata.sql文件里
    2）etcd服务：etcd在这里用于kvServer服务发现，nosServer监控/nos/kvservers目录，在提交object时会到该目录下获取kvServer地址，然后随机选择一个kvServer上传object

4、该系统设计原理如下：
    1）判断请求方法是否是get、put、delete，如果是则继续，否则提示方法不支持，退出
    2）到metadata表中查询该object是否存在，如果存在则返回true及一个纪录object信息的map，如果不存在则返回false及一个空map。（isok, objectInfoMap）
    3）如果是get请求，则
        如果isok == true，则表明object存在，进行如下操作：
            A)从objectInfoMap中取出sha256_code对应的值
            B)从etcd中获取kvServer地址，etcd返回的是包含所有kvServer地址的slice
            C)遍历kvServer的slice，进行广播，起一个channel用于接收返回的数据，
                1、如果1s内有数据返回，则根据返回的kvServer的信息，连接该kvServer，获取数据
                2、如果1s内没有返回数据，则表示获取object数据失败，返回500报错码
        如果isok == false，则表明object不存在，直接返回404
    4）如果是put请求，则
        A)首先判读put请求是否符合要求，在符合要求的情况下到metada表里查询该objectName
        B)如果objectName存在且is_del=0则表示该object存在，返回400(文件已存在)
        C)如果objectName存在且is_del=1或者objectName为空则表示该object不存在，此时需要如下操作：
            1、检查客户端发起的put操作是否符合要求，要求如下：head中必须要有对象的大小、对象的加密方式（该版本只支持sha256），对象的sha256值，否则直接返回报错
            2、如果客户端发起的put操作是否符合要求则将对象临时存放在nos组件的tmp目录下
            3、在本地计算其sha256的值，并与客户端发送过来的sha256值进行对不，如果不相同，则返回报错
            4、如果sha256值相同则以该值为objectName，存入随机选中的kvserver
    5）如果是delete请求，则:
        如果isok == true，则表明object存在，直接在metadata表中软删除该object（将is_del设置为1）
        如果isok == false，则表明object不存在，直接返回404


5、广播过程如下：
    1）从etcd中获取kvserver信息
    2）获取objectName对应的sha256_code，并封装成search请求，并发方式向kvserver进行广播，
    3）如果该kvserver存在该object的数据则将自己的IP地址放入channel中，如果没有则不做任何回应，该版本中最多有且只有一个kvserver会返回一个ok状态
    4）设置一个select，如果超过1s还没有从channel中取到数据，则返回400
    5）对kvserver发起secondOperation操作

6、object状态如下：
    1）objectName存在且is_del=0则表示该object存在
    2）如果objectName存在且is_del=1或者objectName为空则表示该object不存在
